import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

type HeaderProps = {
  logo: string;
  text: string;
  location: string;
  background: string
}

const QikHeader = ({logo, text, location, background}: HeaderProps) => {
  <Header>
    
  </Header>
}

export {QikHeader}